POSTGRES_USER=postgres
POSTGRES_PASSWORD=nagarkot
POSTGRES_DB=lms
POSTGRES_PORT=5432
POSTGRES_HOST=postgres  # This matches the service name in docker-compose.yml

# Database Connection URL (for Node.js)
DATABASE_URL=postgresql://postgres:nagarkot@postgres:5432/lms

# Other Environment Variables (Optional)
NODE_ENV=development 